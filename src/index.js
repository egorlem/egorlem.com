'use strict';
/* LIBS */
import React from "react";
import ReactDOM from "react-dom/client";
/* COMPOENTS */
import { App } from './app';
/* STYLES */
import './styles/index.scss';

// if( window.matchMedia('(prefers-color-scheme: dark)').matches )
// {
//     scheme.innerHTML = 'В ОС установлена тёмная тема';
// }

// if( window.matchMedia('(prefers-color-scheme: light)').matches )
// {
//     scheme.innerHTML = 'В ОС установлена светлая тема';
// }

// console.log('init');

// const rootReactNode = document.getElementById('root');

// function initUserData() {
//   console.log('data seted');
// }

// document.addEventListener('keyup', (event) => {
//   const { code } = event;
//   rootReactNode.innerText = keyCode + 'up'
//   console.log(which);

// })
// document.addEventListener('keydown', (event) => {
//   const { code } = event;
//   rootReactNode.innerText = keyCode + 'down'
// })

// initUserData();

// /* REACT */
// const rootReactNode = document.getElementById('root');
// const root = ReactDOM.createRoot(rootReactNode);
// root.render(
//   <React.StrictMode>
//     <App />
//   </React.StrictMode>
// )


